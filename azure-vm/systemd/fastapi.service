[Unit]
Description=FastAPI Sample
After=network.target

[Service]
User=deployer
WorkingDirectory=/opt/fastapi
Environment=PORT=8080
ExecStart=/opt/fastapi/.venv/bin/uvicorn app:app --host 0.0.0.0 --port 8080
Restart=always

[Install]
WantedBy=multi-user.target
