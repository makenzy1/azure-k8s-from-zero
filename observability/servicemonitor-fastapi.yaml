apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fastapi
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: fastapi
  namespaceSelector:
    matchNames: [ "apps" ]
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
